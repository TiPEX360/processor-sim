The assembler and processor may be built with the two commands respectively:

    make assembler
    make proc

The output programs are in the ./bin directory

Assembly source files may be assembled to binary files using the following

    ./assembler [source path] [output path]

Binary programs assembled by the assembler provided may be executed by proc using the following:

    ./proc [program path]
